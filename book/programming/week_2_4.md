# Testing in Python

This chapter picks up from last week on errors. Now the focus is on checking for error by testing our code. We will first have a look at the use of the `Assert` statement, and how different values are raised. Then, we will look at `Unittest`, a package commonly used to write tests.